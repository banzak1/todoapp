# Sessão 2026-06-26 — Fase 1: CRUD Task + Oh My Zsh

## Resumo

Sessão dividida em duas partes: configuração do terminal (Oh My Zsh) e implementação do CRUD completo da entidade Task seguindo spec-driven development.

---

## 1. Ambiente

### Oh My Zsh

- Zsh 5.9 instalado no WSL2 Ubuntu
- Oh My Zsh instalado com sucesso
- Faltou rodar `chsh -s $(which zsh)` para tornar padrão (precisa de terminal interativo)
- `.zshrc` gerado automaticamente

### VS Code Auto-Import
- `.vscode/settings.json` configurado com auto-import ao completar (como IntelliJ)
- `source.organizeImports` ativado ao salvar

## 2. Fase 1 — CRUD Completo da Task

### Spec-Driven Development

Seguindo o pipeline `tlc-spec-driven`:
1. **Specify** → `.specs/features/task-crud/spec.md` com 15 requisitos traceáveis (TASK-01 a TASK-15)
2. **Design** → Skip (CRUD direto, sem decisão arquitetural nova)
3. **Tasks** → Skip (passos implícitos)
4. **Execute** → Implementado em 4 tasks atômicas

### Commits

```
docs(task-crud): add feature specification
feat(task-crud): implement complete REST CRUD API
```

### O que foi criado

| Camada | Arquivo | Descrição |
|--------|---------|-----------|
| Domain | `Task.java` | @PrePersist/@PreUpdate para timestamps |
| Application | `TaskService.java` | Lógica de CRUD + mapeamento manual |
| Application | `TaskNotFoundException.java` | Exceção customizada |
| Infrastructure | `TaskRepository.java` | Spring Data JPA |
| Interfaces | `TaskController.java` | REST endpoints |
| Interfaces | `GlobalExceptionHandler.java` | Tratamento de erros padronizado |
| Interfaces | `CreateTaskRequest.java` | DTO de criação (record) |
| Interfaces | `UpdateTaskRequest.java` | DTO de atualização (record) |
| Interfaces | `TaskResponse.java` | DTO de resposta (record) |
| Config | `application.yml` | H2 em memória, OpenAPI config |

### Endpoints

| Método | Rota | Status |
|--------|------|--------|
| POST | `/api/v1/tasks` | 201 Created |
| GET | `/api/v1/tasks` | 200 OK |
| GET | `/api/v1/tasks/{id}` | 200 / 404 |
| PUT | `/api/v1/tasks/{id}` | 200 / 404 |
| DELETE | `/api/v1/tasks/{id}` | 204 / 404 |

### Build
- `mvn clean compile` — 12 arquivos, SUCCESS
- `mvn test` — context load, SUCCESS

## 3. Próximos Passos

1. Testes unitários (JUnit 5 + Mockito) para TaskService
2. Testes de integração (Testcontainers + PostgreSQL)
3. Documentação da API via Swagger (já configurado no pom.xml)

---

*Ver também: [[Roadmap]], [[Decisões]], [[Diretrizes]]*
